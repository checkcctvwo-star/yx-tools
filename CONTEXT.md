# yx-tools

Cloudflare 优选 IP 测速工具：从一个或多个 IP 来源中测出速度最快的 Cloudflare IP，并上报给订阅渠道（GitHub 仓库、cfnew Worker 面板）。

## Language

**优选 IP**：
测速后按下载速度降序排在最前的 Cloudflare IP。
_Avoid_: 好 IP、快的 IP

**候选来源**：
一次测速的输入 IP 集合。由 URL 列表、粘贴文本和随机 Cloudflare IP 合成，按 `IP:端口` 去重。
_Avoid_: 输入源、IP 池

**反代模式**：
直接测给定的 `IP:端口` 列表，给什么测什么，不抽样、不穷举网段。
_Avoid_: 代理模式

**定时任务**：
按执行时间自动执行「合成来源 → 测速 → 优中选优上报」的完整流程。每条任务持有自己的参数、来源与上报设置快照。执行时间有两种模式：间隔模式（每 N 分钟/小时）与每天固定时刻模式（`HH:MM` 列表，按服务器本地时钟计算下一次执行）。
_Avoid_: 计划任务、cron 任务

**分地区配额**：
上传时按机场码把结果分组，每个地区取前 N 条（预设 吉隆坡/新加坡/香港/日本/台北 + 手动机场码 + 「其他地区」兜底）。某地区凑不够时按策略处理：不足就少传（默认）或用其他地区补位。配了配额后上传总量 = 各配额之和。
_Avoid_: 按地区上传

**固定附带列表**：
用户指定的 IP 或域名 + 名字，不参与测速，每次上传（手动/定时/命令行）都附加在结果末尾；与测速结果同 IP 的自动去重，保留固定条目。GitHub 里写 `IP:port#名字`（域名默认端口 443），Worker 用名字字段。
_Avoid_: 白名单、强制列表

**下载策略**：
下载测速阶段的结束策略。「测够了就停」（默认）凑够 `-n` 个达标 IP 就收工；「全部测完再挑」把所有通过延迟筛选的候选下载测完再按速度取前 N。
_Avoid_: 测速方式、模式

**优中选优**：
把一轮测速结果按速度降序取前 N 条用于上报，覆盖远端旧列表。未配分地区配额时的默认行为。
_Avoid_: 去重上报

**上报**：
把优选 IP 写到外部目标：GitHub 仓库中的文本文件，或 cfnew 面板的优选 IP 列表。
_Avoid_: 上传、同步

**cfnew**：
配套的 Cloudflare Workers 面板，通过 `{domain}/{uuid}/api/preferred-ips` 读写优选 IP 列表。
